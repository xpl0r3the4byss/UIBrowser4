import Cocoa

/// View controller for update preferences
class UpdatePreferencesViewController: NSViewController {
    override func loadView() {
        view = NSView(frame: NSRect(x: 0, y: 0, width: 450, height: 300))
    }
}